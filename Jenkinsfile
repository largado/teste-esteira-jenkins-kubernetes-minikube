pipeline {
    agent any

    stages {
        
        stage ('fazendo ssh para o servidor do kubernetes e executando deploy no Kubernetes') {
                    steps {
                        script {
                                sh

                        }
                    }

        }
    }
}
